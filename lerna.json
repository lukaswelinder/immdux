{
  "version": "0.0.0",
  "npmClient": "yarn",
  "useWorkspaces": true,
  "command": {
    "publish": {
      "ignoreChanges": ["*.md", "*.test.ts"],
      "message": "chore(release): publish"
    }
  },
  "packages": [
    "packages/*"
  ]
}
