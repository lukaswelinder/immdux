{
  "version": "0.0.0",
  "npmClient": "yarn",
  "command": {
    "publish": {
      "ignoreChanges": ["*.md", "*.test.ts"],
      "message": "chore(release): publish %s"
    }
  },
  "packages": [
    "packages/*"
  ]
}
